### Server
server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8

### DB
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ormJpa?serverTimeZone=Asia/Seoul
    password: root
    username: root

### JPA
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update